# acp-bridge configuration
# All values can be overridden by environment variables.
# When spawned by openab, env vars are sufficient — this file is optional.

[llm]
# OpenAI-compatible endpoint
base_url = "http://localhost:11434/v1"

# Model name
model = "gemma4:26b"

# API key (most local services ignore this)
# api_key = "local-ai"

# Custom system prompt (auto-generated if not set)
# system_prompt = "You are a helpful coding assistant."

# Sampling temperature (0.0 - 2.0)
# temperature = 0.7

# Maximum tokens to generate
# max_tokens = 4096

# HTTP timeout in seconds
timeout_secs = 300

# Max conversation turns to keep (0 = unlimited)
# Prevents memory growth in long sessions
max_history_turns = 50

# ---------------------------------------------------------------------------
# A2A HTTP server (used with --a2a flag)
# ---------------------------------------------------------------------------
# [a2a]
# host = "0.0.0.0"
# port = 8080
#
# Require an "Authorization: Bearer <token>" header on all A2A requests.
# STRONGLY recommended whenever the server is reachable beyond localhost.
# Can also be set via the A2A_AUTH_TOKEN environment variable.
# auth_token = "${A2A_AUTH_TOKEN}"

# ---------------------------------------------------------------------------
# Client mode — spawn an external ACP agent (used with --client flag)
# ---------------------------------------------------------------------------
# [agent]
# command = "opencode"
# args = ["acp"]
# working_dir = "/home/node"
#
# # Optional environment variables passed to the agent process
# [agent.env]
# OPENAI_API_KEY = "${OPENAI_API_KEY}"
